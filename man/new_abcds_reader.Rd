% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_abcds.R
\name{new_abcds_reader}
\alias{new_abcds_reader}
\title{Create ABCDS data reader functions}
\usage{
new_abcds_reader(
  pattern,
  default_cols = NULL,
  .f = NULL,
  include_demographics = TRUE
)
}
\arguments{
\item{pattern}{Character string. Regular expression pattern to match file names.
Uses Perl-compatible regex (PCRE).}

\item{default_cols}{Column specification object created by \code{\link[readr:cols]{readr::cols()}}.
If NULL, readr will guess column types based on the first 2000 rows.}

\item{.f}{Function. Optional post-processing function applied to the data
after reading and merging demographics. Should accept a data frame and
return a modified data frame. Default is NULL (no post-processing).}

\item{include_demographics}{Logical. If TRUE, merges demographic data
(age, gender, race, ethnicity) with the returned data. Default is TRUE.}
}
\value{
A function with the signature:
\verb{function(directory, controls, add_demographics, col_types, ...)}

The returned function accepts:
\describe{
\item{directory}{Character string. Path to the directory containing data files.
If NULL, uses the path set by \code{\link[=set_abcds_directory]{set_abcds_directory()}}.}
\item{controls}{Logical. If TRUE, reads control data; if FALSE, reads
participant data. Default is FALSE.}
\item{add_demographics}{Logical. If TRUE, merges demographic data
(age, gender, race, ethnicity) with the returned data. Default is TRUE.}
\item{col_types}{Column specification for \code{\link[readr:read_delim]{readr::read_csv()}}. Defaults to
the \code{default_cols} specified in the factory.}
\item{...}{Additional arguments passed to \code{\link[readr:read_delim]{readr::read_csv()}}.}
}
}
\description{
This is a function factory that creates reader functions for ABCDS study data files.
It is used internally to generate the various \verb{read_*} functions in this package.
The factory returns a function that reads CSV files matching a specified pattern,
optionally adds demographics, and applies custom post-processing.
}
\details{
The factory creates a reader function that:
\enumerate{
\item Validates the directory path
\item Finds files matching the pattern
\item Ensures only one file matches (aborts if multiple files found)
\item Reads the CSV file with readr
\item Removes the \code{update_stamp} column if present
\item Optionally merges demographics based on \code{subject_label} and \code{event_sequence}
\item Applies custom post-processing function if provided
}

When \code{add_demographics = TRUE}, the function merges age at event and simplified
demographics. If the data contains an \code{event_sequence} column, it joins on both
\code{subject_label} and \code{event_sequence}; otherwise it joins only on \code{subject_label}
using baseline (event_sequence == 1) demographics.
}
\seealso{
\code{\link[cli:cli_abort]{cli::cli_abort()}} for error messages
\code{\link[readr:read_delim]{readr::read_csv()}} for reading CSV files
\code{\link[dplyr:mutate-joins]{dplyr::right_join()}} for merging demographics
}
\keyword{internal}
